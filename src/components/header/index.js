import './navbar.css';

const NavBar = () => {
    return (
        <div className="header">
            <h1 className="header-logo">Today's Weather</h1>
        </div>
    );
}

export default NavBar;